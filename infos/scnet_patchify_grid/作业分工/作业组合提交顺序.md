# 作业分工与组合提交顺序

## 固定清单与初始分工

角色以 `../accounts.csv` 为准；乌镇1500只汇总。`patch_assignment.csv` 给出47个patch的初始logical_owner，同patch的所有model/SSP/tech默认归该owner。实际submit_username可以随空槽调整，科学unit身份不变。

初始权重引用上游 `bcsd/infos/scnet_for_global_bcsd_reRun/config.py` 的历史 `PATCH_LAND_POINTS`，共1,526,295个陆地点；按点数降序（同权重按patch名），依次分给累计权重最少的worker（平局按accounts.csv顺序）。这些是均衡参考值，不是V2实际格点数或耗时实测。全网格最终文件包含矩形网格的域外fill，容量不能只按陆地点乘int8估算。pilot后可按实测调整领取顺序，不改已成功unit。

| 账号 | 用户名 | patch数 | 参考陆地点 | 初始patch |
|---|---|---:|---:|---|
| 乌镇199 | aclym5felp | 6 | 190,663 | R01C01, R02C09, R02C11, R03C09, R05C05, R05C12 |
| 乌镇1892 | acmctdumnv | 6 | 190,303 | R01C02, R01C07, R02C10, R03C10, R03C11, R05C07 |
| 乌镇1520 | acebblgpte | 6 | 190,275 | R01C04, R02C03, R03C03, R03C05, R04C04, R04C12 |
| 乌镇1870 | acjp7am6kd | 6 | 190,692 | R01C10, R02C05, R02C12, R03C07, R04C08, R05C04 |
| 乌镇1959 | acxujo26gv | 5 | 192,857 | R01C05, R01C09, R02C02, R02C08, R04C10 |
| 乌镇1752 | acavkyjhiz | 6 | 189,287 | R01C08, R03C06, R03C08, R04C07, R05C08, R05C11 |
| 乌镇1862 | acdnwyi34a | 6 | 191,088 | R01C03, R01C06, R01C12, R04C05, R04C11, R05C10 |
| 乌镇1359 | ac8svj4ybp | 6 | 191,130 | R01C11, R02C01, R02C04, R02C06, R02C07, R03C04 |

## 库存与本轮范围

8账号各持有同一份4模型库存的13,536个脚本，生成于本账号share，manifest与脚本hash相同。每个GCM实际为282个组合，每组合1baseline、10signals、1audit，共3,384个unit。4模型库存不是本轮分母；先让用户选择GCM，未选择者不提交、不等待。当前只有CANESM5被报告V2完成，也必须先取得本轮选择。

## Pilot及滚动队列

1. 先选用户确认范围内的GCM，在乌镇199运行 R03C09/ssp126/solar baseline，再运行2015–2019 signals。其他组合待pilot通过资源与容量验证再放开。pilot成功文件直接计入正式结果。
2. 常规组合优先级为用户确认的模型顺序、ssp126→ssp245→ssp585、wind→solar；patch内部优先较大参考权重，尽早启动长任务。优先级不是屏障，依赖未满足时跳过该unit检查其他ready项。
3. baseline成功即可释放同组合的10个信号段；段与段没有依赖，可并行。各段会读取自己的时间halo；不裁切输入文件来提速。同组合全部段成功即可释放audit，无需等其他组合结束。
4. 空槽优先补已齐依赖的audit，再轮流领取signals和baseline，避免基线队列把已ready的信号一直排在后面。若某一类暂时无ready项，继续领取另一类。同组合signals按年份升序领取，但允许跨账号、并行执行。
5. 每个账号最多20个account-wide在途作业，其他项目也占槽；pending/configuring/completing等非终态均占槽。每次提交前在统一锁内重计数，有一个槽就可补一个，不等batch整批完成。不使用Slurm数组绕过计数。

## 重分配与失败

logical_owner只用于追溯初始分工。空闲账号可领取其他账号尚未提交、依赖ready的unit，也可领取查明终态后允许重试的unit。领取时记录新submit_username、assignment_version、attempt及原因，直接用目标账号已有同版脚本；资源profile变化需先在8账号生成同一新pack。

同一unit只允许一个active Job；失联、submitting回执丢失或状态unknown先查明，不抢跑副本。已成功结果保持原始物理路径，不复制、不迁移active任务。signals通过EXTREME_BASELINE_FILE读取权威baseline；audit通过EXTREME_COMBINATION_INDEX读取各段的实际路径。新账号只写自己share。

原账号同配置重试可复用有效parts；换账号因parts路径参与身份计算而重新生成该失败unit的parts。不搬运parts伪装续跑。已经成功的其他段照常复用，不按整组合重算。重试分类、次数与资源调整遵循goal.md。

## 监控与汇总

初始15分钟；pilot得到baseline与完整五年signals耗时后，按goal选15/30/60分钟并对齐Asia/Shanghai钟点。每轮检查所有账号、终态和产物，更新按阶段与model/SSP的计数，记录补槽、重分配、失败原因及下次检查时间。不能以余额查询或sshare推测账号可运行时长。

每个组合的audit索引列出原始baseline及10个signals物理路径；1500汇总全部所选组合的audit与receipt。汇总无需复制NC或拼全球大文件，必须实际验证1500能访问全部权威路径。只有所选GCM全部unit成功、无未解决状态时完成本轮。
