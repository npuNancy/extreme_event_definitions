# 极端天气 patchify guide

Use `scnet/create_extreme_patch_jobs.py` to generate scripts only. Check BCSD final
sidecars and patch preparation before submission. The old E1/E2 threshold stages
and non-patch adapters are not dependencies of this campaign.
Pass the active patch IDs explicitly with `--patches` so the job set follows the BCSD manifest.
