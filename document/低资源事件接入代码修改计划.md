# 低资源事件接入代码修改计划

> 根目录：`/data6/yanxiaokai/project_climate/extreme_event_definitions`
>
> 本次修正目标：低资源事件阈值不得再用各模式、各 SSP 自身的 CF 序列计算。应先基于 ERA5Land 的 2015-2025 年风电/光伏 CF 计算固定低资源阈值并保存；后续跑不同模式、不同 SSP 时，只读取这套已保存阈值来判定 `signal_low_resource`。

---

## 1. 当前问题

1. 仓库已有低资源事件入口：
   - `registry.py` 注册了 `LOWRES = {"wind": wind_low_resource, "solar": solar_low_resource}`。
   - `events/wind_low_resource.py`、`events/solar_low_resource.py` 调用 `common.low_resource(...)`。
   - `grid_extreme_signals/cf_low_resource.py` 已能基于 CF 文件计算低资源信号。
   - `grid_extreme_signals/signal_runner.py` 和 `scripts/station_signals_direct.py` 已接入默认低资源计算。

2. 现有阈值计算逻辑不符合本次口径：
   - 当前 `compute_grid_low_resource(...)` / `compute_station_low_resource(...)` 会用当前任务对应的 CF 文件及其 `baseline_years` 直接计算气候态和 P5 阈值。
   - 这意味着 `NESM3/ssp126`、`NESM3/ssp585`、其他 GCM/SSP 都会得到各自的低资源阈值。
   - 该做法会把未来情景自身的气候分布当作阈值基线，导致不同模式/SSP 之间的低资源事件不可比。

3. 正确口径应改为：
   - 阈值来源固定为 ERA5Land。
   - 基线期固定为 `2015-2025`。
   - 阈值预先计算并落盘。
   - 之后所有模式、所有 SSP 只读取同一套 ERA5Land 阈值，不再在情景 CF 上重新估计阈值。

---

## 2. 数据源

ERA5Land 风光 CF 数据目录：

```text
data/cfs/CFs_of_wind_ERA5Land
data/cfs/CFs_of_solar_ERA5Land
```

已确认的文件形态：

```text
wind_cf_YYYY_MM.nc
solar_cf_YYYY_MM.nc
```

示例元数据：

```text
time: hourly, units = seconds since 1970-01-01
lat: 1801, stored_direction = decreasing
lon: 3600
wind_cf(time, lat, lon), units = 1
solar_cf(time, lat, lon), units = 1
```

当前数据完备性检查结果：

```text
solar ERA5Land: 2015-2024 每年 12 个月；2025 当前 11 个月
wind  ERA5Land: 2015-2025 每年 12 个月，已传输完成
```

实施时必须先做文件完整性检查。阈值预计算要求目标期内每个 tech 至少满足：

```text
2015-01 至 2025-12 月文件齐全
```

如果不齐全：

- 光伏可先在数据齐全后再计算正式阈值；临时测试可只跑可用年份，但输出文件必须标明 `baseline_years_effective`。
- 风电当前已满足正式阈值预计算的数据完整性要求。

---

## 3. 低资源事件口径

### 3.1 变量口径

低资源统一使用容量因子 CF：

```text
wind:  wind_cf
solar: solar_cf
```

不再使用旧的气象资源口径：

```text
wind:  wind_ms
solar: rsds
```

输出属性必须明确：

```text
low_resource_source = capacity_factor
low_resource_threshold_source = ERA5Land
low_resource_threshold_baseline_years = 2015-2025
```

### 3.2 计算公式

对 ERA5Land 基线期内每个网格点 `k`，CF 序列记为 `CF(t,k)`。

1. 计算 24h 居中滚动均值：

\[
R(t,k)=\operatorname{mean}\{CF(\tau,k): \tau \in W_{24h}(t)\}
\]

ERA5Land CF 是小时级数据，因此：

```text
window_steps = 24
```

2. 计算月×小时气候态：

\[
C(m,h,k)=\operatorname{mean}\{R(t,k): month(t)=m,\ hour(t)=h,\ t \in 2015\text{-}2025\}
\]

3. 计算 ERA5Land 距平：

\[
A(t,k)=R(t,k)-C(month(t),hour(t),k)
\]

4. 计算每个 ERA5Land 网格点的 P5 阈值：

\[
T(k)=P_5\{A(t,k): t \in 2015\text{-}2025\}
\]

5. 保存 `C(m,h,k)` 和 `T(k)`，后续不再重复估计。

### 3.3 后续模式/SSP 判定

对任意待计算的模式/SSP CF 序列 `CF_model(t,j)`：

1. 计算该模式/SSP 自身的 24h 居中滚动均值 `R_model(t,j)`。
2. 将模式网格点或场站 `j` 匹配到 ERA5Land 阈值网格点 `k`。
3. 使用 ERA5Land 的 `C(m,h,k)` 计算距平：

\[
A_{model}(t,j)=R_{model}(t,j)-C_{ERA5Land}(month(t),hour(t),k)
\]

4. 使用 ERA5Land 的 `T(k)` 判定低资源：

\[
signal\_low\_resource(t,j)=A_{model}(t,j)\le T_{ERA5Land}(k)
\]

注意：不同模式/SSP 只改变 `R_model`，不改变 `C_ERA5Land` 和 `T_ERA5Land`。

### 3.4 光伏夜间处理

光伏继续做夜间剔除，避免夜间 CF 为 0 时被系统性判为低资源：

\[
signal(t,j)=False,\quad solar\_elevation(t,j)\le 0
\]

夜间判断使用目标点位的经纬度和目标时间轴。

### 3.5 `t` 与 `t+1` 标记

保留现有低资源事件定义中的向后扩展一步：

```python
ev2[1:] |= ev[:-1]
```

输出属性写明：

```text
low_resource_mark_next_step = true
low_resource_timestep_hours = <target CF timestep>
```

ERA5Land 阈值预计算阶段是 1h；未来情景 CF 若为 3h，`t+1` 表示下一个 3h 时间步。

---

## 4. 阈值文件设计

新增默认输出目录：

```text
outputs/low_resource_thresholds/ERA5Land_2015-2025
```

建议每个技术一个阈值文件：

```text
low_resource_threshold_wind_ERA5Land_2015-2025.nc
low_resource_threshold_solar_ERA5Land_2015-2025.nc
```

变量：

```text
clim(month, hour, lat, lon)      float32
threshold(lat, lon)              float32
valid_count(lat, lon)            int16 或 int32
```

坐标：

```text
month = 1..12
hour = 0..23
lat
lon
```

推荐全局属性：

```text
tech = wind | solar
threshold_source = ERA5Land
threshold_source_files = data/cfs/CFs_of_<tech>_ERA5Land/*.nc
baseline_years_requested = 2015-2025
baseline_years_effective = 2015-2025
resource_variable = wind_cf | solar_cf
resource_units = 1
window_hours = 24
window_steps = 24
timestep_hours = 1
percentile = 5
climatology = month_hour
created_by = scripts/precompute_low_resource_thresholds.py
```

---

## 5. 代码修改计划

### 5.1 新增 ERA5Land 阈值预计算脚本

新增脚本：

```text
scripts/precompute_low_resource_thresholds.py
```

职责：

1. 读取 `data/cfs/CFs_of_wind_ERA5Land` 和 `data/cfs/CFs_of_solar_ERA5Land` 的月文件。
2. 检查 `2015-01` 至 `2025-12` 文件是否齐全。
3. 按时间顺序拼接月文件，不一次性读入完整 `(time, lat, lon)`。
4. 逐空间块计算：
   - 24h 居中滚动均值；
   - 月×小时气候态 `clim(month,hour,lat,lon)`；
   - ERA5Land 距平；
   - P5 阈值 `threshold(lat,lon)`。
5. 写出阈值 NetCDF。

建议 CLI：

```text
--cf_root data/cfs
--output_dir outputs/low_resource_thresholds/ERA5Land_2015-2025
--baseline_years 2015-2025
--tech both
--lat_chunk 8
--allow_incomplete
```

默认不允许数据不完整。`--allow_incomplete` 只用于调试，正式结果不得使用。

### 5.2 修改 `grid_extreme_signals/cf_low_resource.py`

把当前“直接从任务 CF 估计阈值”的逻辑改为两阶段：

1. 新增阈值读取：

```python
load_low_resource_threshold(threshold_file)
```

读取：

```text
lat, lon, clim, threshold
```

2. 新增目标点匹配 ERA5Land 阈值网格：

```python
match_threshold_grid(threshold_lat, threshold_lon, target_lat, target_lon)
```

要求：

- 复用 `station_match.normalize_grid_lon(...)` / `lon_to_180(...)`。
- 支持 ERA5Land 纬度递减。
- 输出 `threshold_lat_idx` / `threshold_lon_idx` / `threshold_match_dist_deg`。

3. 修改场站低资源计算：

```python
compute_station_low_resource(..., threshold_file=...)
```

核心变化：

- 目标 CF 文件只用于计算 `R_model(t,j)`。
- `clim_tbl` 和 `thr` 来自 ERA5Land 阈值文件。
- 不再使用目标 CF 的 `baseline_years` 估计阈值。

4. 修改网格低资源计算：

```python
compute_grid_low_resource(..., threshold_file=...)
```

核心变化：

- 对目标模式网格每个格点匹配 ERA5Land 阈值格点。
- 使用匹配后的 `clim(month,hour)` 和 `threshold` 判定事件。
- 分块处理，避免同时载入全球阈值和目标 CF 全量数据。

5. 保留兼容性：

- 可以保留旧参数 `baseline_years`，但只作为弃用兼容参数。
- 若未传 `threshold_file`，应报错或明确 warning 后走 legacy 行为；正式 pipeline 必须传 `threshold_file`。

### 5.3 修改 `common.py`

当前 `common.low_resource(...)` 已支持传入 `clim_tbl` 和 `thr`，但注释仍强调“便捷近似”。需要改成正式支持外部阈值：

1. 明确 `clim_tbl/thr` 可以来自外部基线。
2. 当传入 `clim_tbl/thr` 时，不使用 `base_mask` 估计阈值。
3. 继续支持：

```text
window_steps = 24 for hourly ERA5Land
window_steps = 8  for 3-hour target CF
```

4. 单元测试覆盖 `clim_tbl/thr` 外部传入路径。

### 5.4 修改 Pipeline B：`scripts/station_signals_direct.py`

CLI 改为：

```text
--cf_root data/cfs
--lowres_threshold_dir outputs/low_resource_thresholds/ERA5Land_2015-2025
--no_low_resource
```

删除或废弃：

```text
--lowres_baseline_years
```

处理流程：

1. 当前 weather 简单事件保持不变。
2. 默认计算 `low_resource`。
3. 根据 `tech` 选择阈值文件：

```text
wind  -> low_resource_threshold_wind_ERA5Land_2015-2025.nc
solar -> low_resource_threshold_solar_ERA5Land_2015-2025.nc
```

4. 读取目标模式/SSP 的 CF 到场站。
5. 场站经纬度匹配到 ERA5Land 阈值网格。
6. 使用 ERA5Land `clim/thr` 判定低资源。
7. 仍应用：
   - `activation_year` 掩膜；
   - `match.valid` 或 `match_dist_deg` 掩膜；
   - 光伏夜间剔除。

输出 attrs 增加：

```text
low_resource_threshold_file = ...
low_resource_threshold_source = ERA5Land
low_resource_threshold_baseline_years = 2015-2025
low_resource_threshold_match_max_dist_deg = ...
```

Pipeline B 是优先改造对象，因为只处理场站点位，内存风险低于全网格。

### 5.5 修改网格 Pipeline：`grid_extreme_signals/signal_runner.py`

CLI 参数由 `scripts/generate_multi_source_grid_signals.py` 传入：

```text
--cf_root data/cfs
--lowres_threshold_dir outputs/low_resource_thresholds/ERA5Land_2015-2025
--no_low_resource
```

处理流程：

1. 原有简单事件不变。
2. 默认计算 `low_resource`。
3. 根据当前 `tech` 读取 ERA5Land 阈值文件。
4. 根据目标网格经纬度匹配 ERA5Land 阈值网格。
5. 分空间块读取目标模式/SSP CF，计算 `signal_low_resource`。
6. 输出 `signal_low_resource(time, lat, lon)` 或 NAM-12 的对应网格维度。

注意：

- 区域 BCSD、China CMFD BCSD、NAM-12 的 CF 网格都不得再单独估计阈值。
- 如果阈值文件缺失，默认应跳过低资源并记录 `skipped_event_reasons`；也可提供严格模式直接报错。

### 5.6 修改 Pipeline A：`scripts/station_signals_from_grid.py`

Pipeline A 抽取阶段不重新计算低资源。

要求：

1. 如果网格文件已有 `signal_low_resource`，像其他 `signal_*` 一样抽取到场站。
2. 如果用户要求低资源但网格文件缺少 `signal_low_resource`，明确报错，提示重新运行网格阶段并提供 ERA5Land 阈值文件。
3. 不在抽取阶段用场站 CF 临时补算，避免 Pipeline A 与 Pipeline B 语义混淆。

### 5.7 修改临时补写脚本

现有：

```text
scripts/patch_pipelineB_low_resource.py
```

需要同步修改为读取 ERA5Land 阈值文件，而不是对每个 SSP 的 CF 文件重新估计 `baseline_years`。

默认参数改为：

```text
--threshold_dir outputs/low_resource_thresholds/ERA5Land_2015-2025
--cf_root data/cfs
--tech both
--overwrite
```

写入 attrs 时必须覆盖旧含义：

```text
low_resource_threshold_source = ERA5Land
low_resource_threshold_baseline_years = 2015-2025
```

---

## 6. 输出变化

### 6.1 新增阈值文件

```text
outputs/low_resource_thresholds/ERA5Land_2015-2025/
  low_resource_threshold_wind_ERA5Land_2015-2025.nc
  low_resource_threshold_solar_ERA5Land_2015-2025.nc
```

### 6.2 网格级输出

新增或保留：

```text
signal_low_resource(time, lat, lon)
```

属性必须能追溯阈值来源：

```text
low_resource_source = capacity_factor
low_resource_cf_file = <target model/ssp CF file>
low_resource_threshold_file = <ERA5Land threshold file>
low_resource_threshold_source = ERA5Land
low_resource_threshold_baseline_years = 2015-2025
```

### 6.3 场站级输出

新增或保留：

```text
signal_low_resource(time, station)
```

属性同样记录 ERA5Land 阈值来源。变量类型仍为 `int8`，取值 `0/1`。

---

## 7. 测试计划

### 7.1 数据完整性测试

新增测试或脚本检查：

1. `CFs_of_solar_ERA5Land` 是否覆盖 `2015-01` 至 `2025-12`。
2. `CFs_of_wind_ERA5Land` 是否覆盖 `2015-01` 至 `2025-12`。
3. 缺文件时默认失败并列出缺失月份。
4. `--allow_incomplete` 时允许继续，但输出 attrs 必须写 `baseline_years_effective`。

### 7.2 阈值预计算单元测试

使用小型合成 ERA5Land 月文件覆盖：

1. 小文件按月拼接后时间轴连续。
2. 1h 时间步推断 `window_steps=24`。
3. 输出包含 `clim(month,hour,lat,lon)` 和 `threshold(lat,lon)`。
4. `threshold` 等于 ERA5Land 距平的 P5。
5. 纬度递减时匹配结果正确。

### 7.3 外部阈值判定测试

构造目标模式 CF 与 ERA5Land 阈值文件：

1. `compute_station_low_resource(..., threshold_file=...)` 不使用目标 CF 重新估计阈值。
2. 修改目标 CF 的未来年份分布不会改变 `threshold`。
3. 光伏夜间信号为 0。
4. `mark_next_step=True` 生效。

### 7.4 Pipeline B 小样本测试

1. `scripts/station_signals_direct.py` 默认输出 `signal_low_resource`。
2. 输出 attrs 指向 ERA5Land 阈值文件。
3. `activation_year` 前置时间为 0。
4. `match_dist` 超阈值场站为 0 或无效。

### 7.5 实数据冒烟测试

优先选择一个小区域：

```text
source = regional_bcsd
model = NESM3
region = Germany
scenario = ssp126
tech = solar / wind
year = 2015
```

检查：

1. 低资源事件比例不是全 0 / 全 1。
2. 光伏夜间未被判为低资源。
3. 输出时间轴与其他事件一致。
4. Pipeline A 与 Pipeline B 在同一场站上的结果一致或差异可解释。

---

## 8. 风险与处理

1. **ERA5Land 全球小时 CF 文件很大**
   - 单月变量约 `(720/744, 1801, 3600)`。
   - 预计算必须按空间块读取，不能全量载入内存。

2. **滚动窗口跨月**
   - 24h 居中滚动需要前后月份边界数据。
   - 按月处理时必须读取相邻月份边界，或先实现按空间块跨完整时间轴流式处理。

3. **ERA5Land 文件未传完或后续重传**
   - 预计算前必须按技术检查 `2015-01` 至 `2025-12` 月文件完整性。
   - 当前风电已完整；光伏若仍缺月，只能生成调试阈值，不能作为正式光伏低资源结果。

4. **ERA5Land 与目标 CF 网格不同**
   - 阈值网格匹配必须记录距离。
   - 对超出范围或距离异常的点，跳过或置 0，并写入 skipped/invalid reason。

5. **1h 阈值 vs 3h 目标 CF**
   - ERA5Land 阈值的 `clim(month,hour)` 是 24 个小时槽。
   - 3h 目标 CF 只会访问其中 8 个小时槽，这是允许的。
   - 目标 CF 的 24h 滚动窗口仍按自身时间步推断，3h 数据使用 8 步。

6. **旧输出属性语义污染**
   - 旧版本 attrs 里的 `low_resource_baseline_years=2015-2029` 代表目标 CF 内部基线。
   - 新版本必须改为 `low_resource_threshold_baseline_years=2015-2025`，避免混淆。

---

## 9. 实施顺序

1. 新增 ERA5Land 文件完整性检查函数和测试。
2. 新增 `scripts/precompute_low_resource_thresholds.py`，先用合成小数据验证。
3. 在真实 ERA5Land 数据齐全后生成光伏、风电阈值文件。
4. 修改 `cf_low_resource.py`，支持读取外部 ERA5Land 阈值。
5. 修改 `common.py` 注释和测试，明确 `clim_tbl/thr` 外部阈值路径。
6. 先改 Pipeline B 和 `patch_pipelineB_low_resource.py`，用 ERA5Land 阈值补算场站结果。
7. 再改网格 runner 和 Pipeline A。
8. 更新 README / 输入输出说明，删除“低资源暂缓”和“目标 CF 自身基线”的旧表述。

---

## 10. 待确认事项

1. ERA5Land 阈值文件统一放在：

```text
outputs/low_resource_thresholds/ERA5Land_2015-2025
```

2. 阈值匹配最大距离沿用当前 `station_match.MAX_DIST_DEG`。
